apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fastchat-api-server.fullname" . }}-test-connection"
  labels:
    {{- include "fastchat-api-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fastchat-api-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
