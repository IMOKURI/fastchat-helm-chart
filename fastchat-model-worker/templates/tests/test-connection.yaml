apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fastchat-model-worker.fullname" . }}-test-connection"
  labels:
    {{- include "fastchat-model-worker.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fastchat-model-worker.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
