apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fastchat-controller.fullname" . }}-test-connection"
  labels:
    {{- include "fastchat-controller.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fastchat-controller.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
